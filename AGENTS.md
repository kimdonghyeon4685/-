# AGENTS.md

이 문서는 사람 개발자와 AI 코딩 에이전트가 이 저장소에서 지켜야 할 공통 작업 원칙입니다.

## 0. ChatGPT 프로젝트 자료 보호

- 이 디렉터리는 ChatGPT 프로젝트 “조상땅찾기”의 로컬 미러이기도 하다.
- `sources/` 아래의 모든 파일은 읽기 전용 참고 자료로 취급한다.
- 동기화된 프로젝트 파일을 수정, 이름 변경, 이동 또는 삭제하지 않는다.
- 동기화 자료는 새 작업 생성 시 교체될 수 있으므로 애플리케이션 소스와 분리해 다룬다.

## 1. 개발 환경과 호환성

- 이 프로젝트는 여러 Windows/macOS 컴퓨터에서 병행 개발한다.
- OS 종속적인 절대경로, 셸 전용 스크립트, 특정 사용자 홈 디렉터리를 코드에 넣지 않는다.
- 경로는 프로젝트 루트 기준 상대경로와 Node.js/Next.js의 크로스 플랫폼 API를 사용한다.
- 의존성은 npm과 `package-lock.json`으로 고정한다. `package-lock.json`은 반드시 Git에 포함한다.
- 정상적인 첫 실행 기준은 `npm install` 후 `npm run dev`이다.

## 2. UI 변경 원칙

- 기존 UI를 수정할 때 사용자가 요청하지 않은 디자인 변경은 최소화한다.
- 공통 컴포넌트, 반응형 동작, 접근성, 기존 정보 구조를 우선 보존한다.
- PC 검색결과는 테이블, 모바일 검색결과는 카드 형태라는 기본 원칙을 유지한다.
- DEMO 데이터라는 표시와 실제 권리관계를 보증하지 않는 안내를 제거하지 않는다.

## 3. 보안과 비밀정보

- `.env`, `.env.local`, API 키, 결제 비밀키, 서비스 역할 키 등 secret을 Git에 commit하지 않는다.
- 공개 가능한 환경변수만 `NEXT_PUBLIC_` 접두사를 사용한다.
- `.env.example`에는 실제 값이 아닌 변수명과 설명만 남긴다.
- 결제 전 민감한 상세 토지정보를 클라이언트에 전달하지 않는 구조를 유지한다.
- 리, 정확한 소재지, 지번, 지목, 면적, 상세 출처정보는 무료 검색 API/Server Component props에 포함하지 않는다.
- CSS blur, `display: none`, DOM 마스킹만으로 유료 필드를 보호하지 않는다.
- 상세 조회는 서버에서 현재 사용자의 구매 권한과 `record_id`를 확인한 뒤 처리한다.
- 클라이언트가 보낸 결제금액을 신뢰하지 말고 서버에서 `선택 건수 × 20,000원`을 재계산한다.

## 4. Git 작업 원칙

- 기준 원격 저장소는 `https://github.com/kimdonghyeon4685/-.git`이며 기본 추적 브랜치는 `origin/main`이다.
- 모든 작업을 시작하기 전에 현재 브랜치와 `git status`를 확인한다.
- 로컬 변경사항이 안전한지 확인한 뒤 `git pull --ff-only`로 원격 저장소의 최신 내용을 받는다.
- pull이 로컬 변경이나 충돌 때문에 불가능하면 stash, discard, rebase 또는 충돌 해결을 임의로 하지 않고 사용자에게 먼저 알린다.
- 작업이 끝나면 변경사항과 `git diff`를 검토하고 필요한 품질 검사를 수행한다.
- 검토가 끝난 변경사항은 작업 내용을 설명하는 적절한 메시지로 commit한 뒤 추적 원격 브랜치에 push한다.
- push 후 로컬 브랜치와 원격 브랜치의 동기화 상태를 확인한다.
- 기존 Git 히스토리를 유지하고 force push를 금지한다.
- 기존 파일이나 다른 작업자의 변경사항을 임의로 덮어쓰지 않는다.
- 충돌이 발생하면 해결을 시도하기 전에 충돌 파일명을 사용자에게 알리고 지시를 기다린다.
- 큰 작업은 기능 단위 브랜치와 작은 commit으로 나눈다.

## 5. 품질 확인

중요한 작업 후 아래 명령을 모두 확인한다.

```bash
npm run lint
npm run build
```

가능하면 아래도 함께 실행한다.

```bash
npm run typecheck
```

오류가 발생하면 원인을 해결한 뒤 다시 실행한다. 검증하지 못한 항목이 있다면 완료 보고에 이유를 명시한다.

## 6. 아키텍처 원칙

- `src/server`의 데이터 접근·결제·권한 로직과 `src/components`의 UI를 분리한다.
- Mock 구현은 향후 Supabase/PostgreSQL, Toss Payments로 교체할 수 있도록 인터페이스 경계를 유지한다.
- 공개 레코드 타입과 상세 레코드 타입을 분리한다.
- 결제 성공 상태의 최종 근거는 브라우저 `localStorage`가 아니라 서버 저장소여야 한다.
- Phase 1의 서버 메모리 세션은 개발 서버 재시작 시 초기화될 수 있다. 이를 영구 저장소로 오인하지 않는다.
- 실제 상용화 전 데이터 출처, 상업적 이용권한, 개인정보, 전자상거래 및 환불 정책을 검토한다.
